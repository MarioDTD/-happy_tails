<div class="cards">
  <% pets.each do |pet| %>
    <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= pet.image_url%>)">
      <%= pet.name %>
    </div>
  <% end %>
</div>
